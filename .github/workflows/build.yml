name: Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Build
      run: make
    
    - name: Test run
      run: ./engine ./sample_bot ./sample_bot ./sample_bot ./sample_bot ./sample_bot
